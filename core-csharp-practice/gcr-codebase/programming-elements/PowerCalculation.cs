using System;
class PowerCalculation{
    static void Main(){
        double baseNum=2.0;
        double exponent=3.0;
        double result=Math.Pow(baseNum,exponent);
        Console.WriteLine(result);
    }
}